<div class="relations_item">
<%= content_tag :h4, title %>
<% ministries.each do |ministry| %>
<div class="collection_search_result">
<%= link_i ministry.Ministry_title, ministry %>
</div>
<% end %>
<%= paginate ministries, :param_name => param_name, :per_page => 5, :browse_uri => browse_uri %>
</div>
