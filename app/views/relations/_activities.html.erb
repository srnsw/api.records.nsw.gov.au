<div class="relations_item">
<%= content_tag :h4, title %>
<% activities.each do |activity| %>
<div class="collection_search_result">
<%= link_i activity.Activity_title, activity %>
</div>
<% end %>
<%= paginate activities , :param_name => param_name, :per_page => 5, :browse_uri => browse_uri %>
</div>
