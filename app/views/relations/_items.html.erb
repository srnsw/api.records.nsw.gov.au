<div class="relations_item">
<%= content_tag :h4, title %>
<% items.each do |item| %>
<div class="collection_search_result">
<%= link_i item.Item_title, item %>
</div>
<% end %>
<%= paginate items, :param_name => param_name, :per_page => 5, :browse_uri => browse_uri %>
</div>
