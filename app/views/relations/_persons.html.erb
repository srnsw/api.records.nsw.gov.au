<div class="relations_item">
<%= content_tag :h4, title %>
<% persons.each do |person| %>
<div class="collection_search_result">
<%= link_i person.name_long, person %>
</div>
<% end %>
<%= paginate persons, :param_name => param_name, :per_page => 5, :browse_uri => browse_uri %>
</div>
