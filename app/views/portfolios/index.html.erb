<h1>Portfolios</h1>
<% @portfolios.each do |portfolio| %>
<%= link_p portfolio.Portfolio_title, portfolio %>
<% end %>
<%= paginate @portfolios, :browse_uri => nil %>

<% content_for :title do %>
<% "Portfolios" %>
<% end %>