<ListRecords>
<% for hit in @list.hits %>
<%= render :partial => "header", :object => hit.result, :locals => {:class_name => hit.class_name, :id => hit.stored(:title), :modified => hit.stored(:modified)} %>
<% end %>
<%= make_resumption_token %>
</ListRecords>
