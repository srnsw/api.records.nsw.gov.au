<header>
  <identifier><%= oai_id(class_name, id) %></identifier>
  <datestamp><%= (date = date_simple(modified)) ? date : "1996-08-26" %></datestamp>
  <setSpec><%= to_set_spec(class_name) %></setSpec>
</header>
